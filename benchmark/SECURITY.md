# Секреты

Не коммитьте `config/benchmark.toml`, `.env`, пароли, сертификаты, внутренние адреса и дампы окружения. Конфигурация хранит только имя переменной с паролем; значение читается во время запуска.
